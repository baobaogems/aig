# ⚠️ MOVED

The content for the 21/05 post lives in `content/day-00-published/` now.

**Why**: After re-classification, the 21/05 post is the "Day 0 preview" (pre-launch teaser). Day 01 in v2 calendar = 26/05 (today) = a fresh post.

**Action for anh**: When convenient, delete this `day-01-published/` folder via Finder. Em không xóa được do permission.

The Day 01 post (fresh, for 26/05) will be tracked at `content/day-01-published/` AFTER anh đăng — when anh chạy `./scripts/scaffold-day.sh 1` again. For now this folder is a stub.
