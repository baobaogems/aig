// Shared surface for every dashboard content card: white panel, hairline border,
// one soft shadow. Kept in one place so the cards cannot drift apart.
export const cardClass =
  "bg-white border border-[var(--color-border-light)] shadow-[0_1px_1.75px_0_rgb(0_0_0/0.051)]";
